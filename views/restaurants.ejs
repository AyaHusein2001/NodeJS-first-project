<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
  <!-- including the header file using ejs special syntax -->
  <%- include('includes/header') %><!-- note! without the extension , you can pass any arguements like this : 
  include('includes/header',{nrests: ...})
  -->
  <%- include('includes/side-drawer') %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if (numberOfRestaurants===0) { %>
      <p>
        Unfortunately , we have not any restaurants yet- maybe recommending some
        ?
      </p>

      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <!-- numberOfRestaurants is a variable that is written in ejs format , to be available then -->
      <p>We found <%= numberOfRestaurants %> restaurants.</p>
      <button class="btn">change order</button>
      <ul id="restaurants-list">
        <!-- here we write js code between these ejs prases , we wrote a normal 
        for loop to loop over all the restaurants and create list item for everyone
        , so this ejs is helpful in rendering repeated content
        -->
        <!--this restaurants array is to be passed later in app.js file-->
        <% for (const restaurant of restaurants) { %>
          <%- include('includes/restaurants/restaurant-item',{restaurant:restaurant}) %>
        <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
